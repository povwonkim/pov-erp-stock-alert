# POV 재고마스터 앱 — 데이터 스키마 · 팀별 사용

재고 판단(품절/발주/수입/단종)과 신규예정제품 출시 준비를 한 곳에서 굴리기 위한 데이터 모델입니다.
구글시트 두 개(이카운트 재고 마스터 · 온라인 재고 리포트)는 **읽기 원본으로 그대로 두고**,
사람의 판단과 일정만 앱이 소유합니다.

**이 문서가 정하는 것** — 테이블 5개, 키, 상태 전이, 팀별 화면·입력·알림.
**이 문서가 정하지 않는 것** — UI 구현, 프레임워크, 배포. 그건 스키마가 확정된 뒤입니다.

---

## 0. 왜 시트가 아니라 앱인가

시트로 안 되는 게 정확히 네 가지입니다. 이것 말고는 시트가 낫습니다.

| | 시트가 못 하는 이유 |
|---|---|
| 판단 상태 보존 | `write_gsheet_report.py:580` `write_sheet()`가 탭을 **통째로 clear 후 재작성**합니다. 사람이 적은 값은 다음날 아침에 사라집니다 |
| 재등장 차단 | 발주해도 재고는 0이라 내일 품절 리스트에 또 올라옵니다. "처리했음"을 기억할 곳이 없습니다 |
| 입고 자동 종결 | 이카운트 재고가 0 → 양수로 바뀌는 순간을 감지해 발주 건을 스스로 닫아야 합니다 |
| 공정 의존·역산 | 오픈일에서 역산한 팀별 마감일과 지연 감지 |

그래서 **데이터 흐름은 단방향입니다. 시트 → 앱.** 앱은 시트에 쓰지 않습니다.

```
이카운트 ──▶ POV_재고관리_마스터 시트 ──┐
                                        ├──▶ 앱 (판단·일정 소유) ──▶ Slack
Cafe24  ──▶ 온라인 재고 리포트 시트  ──┘         │
                                                 └──▶ 단종 목록 ──▶ 알림 파이프라인 제외
```

마지막 화살표가 중요합니다. 앱에서 단종 처리한 품목은 시트와 Slack 알림에서도 빠져야 합니다.
지금 `cafe24_stock_alert.py`의 `EXCLUDED_PRODUCT_CODES`에 3개가 하드코딩돼 있는데, 그 자리를
앱의 단종 목록이 대신합니다.

---

## 1. 키 설계 — 새 코드를 발번하지 않습니다

**결론: 이카운트 품목코드를 마스터 키로 씁니다.** 실측 결과 조인 가능한 상태입니다.

Cafe24 `state.json` 4,018개 variant 기준:

| | 건수 | 비율 |
|---|---:|---:|
| `custom_variant_code` 있음 (= 시트의 `ERP코드`) | 4,003 | **99.6%** |
| 없음 | 15 | 0.4% |
| 중복 (한 코드가 여러 variant) | 4종 10건 | — |

`write_gsheet_report.py:155` `_erp_price()`가 이미 `custom_variant_code → custom_product_code`
순으로 ERP코드를 뽑아 시트에 쓰고 있습니다. 즉 조인은 이미 돌고 있고, 예외가 25건뿐입니다.

```
품목코드 (PK)
  ├── Cafe24  custom_variant_code  ─ 99.6% 자동
  ├── Cafe24  product_no / variant_code  ─ 링크·API용
  └── 이카운트 품목코드            ─ 동일 문자열 가정, probe로 확정 필요
```

**신규예정제품은 이카운트에 품목을 먼저 등록합니다.** 그러면 Cafe24에 상품이 만들어지기 전에도
키가 존재합니다. 이게 파이프라인이 성립하는 조건입니다 — 앱이 별도 키를 발번하면 나중에 이카운트
코드와 다시 맞춰야 하므로 그 길은 택하지 않습니다.

**예외 25건은 `코드예외` 표로 수동 관리합니다.** (미기입 15 + 중복 4종 10) 자동화하기엔 적고,
방치하면 조용히 누락되는 크기입니다.

---

## 2. 테이블

### T1. `품목마스터` — 모든 것의 척추

품목당 1행. **사람이 쓰고 앱이 읽습니다.** 신상품이 생기면 여기부터 추가합니다.

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `품목코드` | text PK | ● | 이카운트 품목코드 |
| `품목명` | text | ● | |
| `브랜드` | text | ● | |
| `카테고리` | enum | ● | 온라인팀 과제 "카테고리별 발주/수입/단종 기준"의 기준 축 |
| `조달유형` | enum | ● | 자체제작 / 국내사입 / 해외수입 / 위탁 |
| `리드타임(일)` | int | ● | 비면 `기준표`의 조달유형 기본값 사용 |
| `MOQ(수량)` | int | | 최소 발주 수량 |
| `MOQ(금액)` | int | | 최소 발주 금액 |
| `입고단가` | int | | |
| `판매가` | int | | Cafe24 `price`에서 동기화 |
| `라이프사이클` | enum | ● | 기획중 / 출시예정 / 판매중 / 단종 |
| `단종일` | date | | `라이프사이클=단종`일 때 필수 |
| `단종사유` | text | | |
| `담당MD` | text | | |
| `cafe24_variant_code` | text | | 자동 매핑 |
| `cafe24_product_no` | int | | 관리자 딥링크용 |
| `온라인판매여부` | bool | ● | false면 온라인 품절 판단 대상 제외 |

**`조달유형`이 온라인팀 분류를 자동화합니다.** 온라인팀이 "발주 / 수입 / 성수점"으로 나눠달라고
한 것 중 앞의 둘은 조달유형 그 자체입니다 — 자체제작·국내사입 → 발주, 해외수입 → 수입.
온라인팀 메모에 *"수입 관련해서는 우선 자사몰 정보 참고하여 정리한 거라서 상품 목록 확인해주시면
감사하겠습니다"* 라고 적힌 부분이 이 컬럼 하나로 대체됩니다. 사람이 매번 추측하지 않습니다.

**`라이프사이클=단종`이 알림 제외의 단일 출처입니다.** 앱·시트·Slack이 모두 이 값을 읽습니다.

---

### T2. `출시공정` — 신규예정제품, 팀별 한 행씩

**품목 × 공정 = 1행.** 한 품목이 오픈까지 8행을 갖습니다. 이렇게 나눠야 각 팀이 자기 행만 필터해
보고, 지연이 공정 단위로 잡힙니다. 와이드 테이블(포토일정·텍스트일정·상세일정을 한 행에 나열)로
만들면 담당자·상태를 공정별로 못 답니다.

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `품목코드` | text FK | ● | → T1 |
| `공정` | enum | ● | 아래 8단계 |
| `담당팀` | enum | ● | |
| `담당자` | text | | |
| `계획일` | date | ● | 오픈예정일에서 자동 역산, 수동 조정 가능 |
| `완료일` | date | | 채워지면 완료 |
| `상태` | enum | ● | 대기 / 진행 / 완료 / 보류 |
| `산출물` | url | | 촬영 원본·상세페이지 시안 링크 |
| `메모` | text | | |

공정 8단계와 의존 관계:

| # | 공정 | 담당팀 | 선행 조건 |
|---|---|---|---|
| 1 | 기획확정 | MD·디자인팀 | — |
| 2 | 발주·생산 | 디자인팀(자체제작) / 물류재고팀(수입·사입) | 1 |
| 3 | 샘플입고 | 물류재고팀 | 2 |
| 4 | 촬영 | 포토 | 3 |
| 5 | 텍스트 | 컨텐츠팀 | 3 |
| 6 | 상세페이지 | 컨텐츠디자이너 | **4 + 5** |
| 7 | 마케팅준비 | 마케팅팀 | 4 (이미지 필요) |
| 8 | 온라인등록·오픈 | 온라인팀 | 6 |

```
기획확정 ─▶ 발주·생산 ─▶ 샘플입고 ─┬─▶ 촬영 ──┬─▶ 상세페이지 ─▶ 온라인등록·오픈
                                    └─▶ 텍스트 ┘                      ▲
                                              마케팅준비 ─────────────┘
```

**6번이 4·5 양쪽을 기다립니다.** 여기가 매번 막히는 지점이라 자동 역산의 값어치가 여기서 나옵니다.

**계획일 역산 (기본값, `기준표`에서 조정)**

| 공정 | 오픈일 기준 |
|---|---|
| 온라인등록·오픈 | D-0 |
| 상세페이지 완료 | D-3 |
| 촬영 완료 · 텍스트 완료 | D-10 |
| 마케팅준비 완료 | D-5 |
| 샘플입고 | D-17 |
| 발주·생산 착수 | D-17 − 리드타임 (자체제작 35 / 해외수입 21 / 국내사입 7) |
| 기획확정 | 그 이전 |

자체제작이면 오픈일 **52일 전**에 발주가 나가야 합니다. 이 숫자가 자동으로 뜨는 것만으로도
디자인팀 일정이 앞당겨집니다.

---

### T3. `판단상태` — 앱이 소유하는 유일한 원본

품절·위험 리스트에 오른 품목의 처리 이력. **한 품목이 동시에 하나의 열린 건만 갖습니다.**

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `건번호` | id PK | ● | |
| `품목코드` | text FK | ● | → T1 |
| `진입일` | date | ● | 리스트에 처음 오른 날 |
| `진입사유` | enum | ● | 위험 / 주의 / 재고소량 / 신규품절 / 지속품절 |
| `즉시조치` | enum | ● | 성수점이동 / 없음 |
| `이동수량` | int | | |
| `이동일` | date | | |
| `보충결정` | enum | ● | 미정 / 발주예정 / 수입검토 / 보류 / 단종 |
| `결정자` | text | | |
| `결정일` | date | | |
| `재검토일` | date | | **`보충결정=보류`면 필수** |
| `발주일` | date | | |
| `발주수량` | int | | |
| `입고예정일` | date | | 발주일 + 리드타임 자동 |
| `상태` | enum | ● | 판단대기 / 결정됨 / 입고대기 / 종결 |
| `종결일` | date | | |
| `종결사유` | enum | | 입고확인 / 단종 / 자연해소 |

**`즉시조치`와 `보충결정`이 별도 컬럼인 이유.** 온라인팀이 직접 짚었습니다 —
*"발주/수입과 중복될 수 있습니다. \*발주 전 온라인 품절을 대체하기 위함"*.
성수점 이동은 임시방편이고 발주는 근본 해결입니다. 하나의 드롭다운으로 묶으면 성수점 이동을
고른 순간 근본 발주가 리스트에서 사라집니다. **리스트 이탈은 `보충결정`만으로 판단합니다.**

**`재검토일`이 누적 모델의 유일한 안전장치입니다.** 무기한 보류를 허용하면 리스트는 조용히
썩습니다. 보류 선택 시 날짜를 강제하고, 그날이 되면 `상태`를 `판단대기`로 되돌립니다.

**`입고대기`가 `종결`과 분리돼 있습니다.** 발주완료는 끝이 아닙니다. 재고는 여전히 0이고
품절은 계속됩니다. 이카운트 재고가 0 → 양수가 되면 앱이 `종결 / 입고확인`으로 자동 전환하고,
`입고예정일`이 지나도 재고가 안 들어오면 알림을 냅니다. 온라인팀 과제인
*"인기상품 1주 이상 품절 지속되지 않도록"* 이 여기서 풀립니다.

---

### T4. `기준표` — 코드가 아니라 숫자표

온라인팀 과제 6개는 전부 로직이 아니라 상수입니다. 코드에 박으면 조정할 때마다 개발이 필요합니다.
**팀이 직접 고치고 다음 실행에 반영됩니다.**

| 컬럼 | 설명 |
|---|---|
| `카테고리` | `*` = 전체 기본값 |
| `조달유형` | `*` = 전체 기본값 |
| `위험선DOI(일)` | 이 아래면 위험 |
| `주의선DOI(일)` | |
| `기본리드타임(일)` | T1에 값이 없을 때 |
| `MOQ(수량)` | |
| `단종기준_무판매(일)` | 이 기간 판매 0이면 단종 후보 |
| `성수점이동_최소수량` | 이보다 적으면 이동 제안 안 함 |
| `앱진입제외_품절(일)` | 기본 30 |
| `앱진입제외_판매(개)` | 기본 0 — 아래 게이트 참고 |

가장 구체적인 행부터 매칭합니다: `(카테고리, 조달유형)` → `(카테고리, *)` → `(*, 조달유형)` → `(*, *)`.

---

### T5. `일별스냅샷` — 판단의 근거

품목 × 날짜. 시트에서 매일 적재합니다. `재고` `7일 판매` `60일 판매` `DOI` `품절일수` `상태`.

이미 `sales_history.json`이 60일 롤링 윈도우로 하고 있는 일입니다. 앱은 이걸 읽기만 합니다.

---

## 3. 진입 게이트 — 첫날 죽지 않게

2026-07-19 스냅샷 실측입니다.

| 탭 | 건수 | 60일 판매 0 |
|---|---:|---:|
| 위험 | 25 | 0 |
| 주의 | 82 | 0 |
| 재고소량·판매없음 | 25 | 0 |
| 신규 품절 | 79 | 0 |
| 지속 품절 | 140 | 0 |
| **장기 품절** | **1,779** | **1,671 (94%)** |
| 합계 | 2,130 | 1,671 |

누적 모델을 그대로 켜면 **2,130건으로 시작하고 그중 1,671건이 60일간 한 개도 안 팔린 SKU**입니다.
아무도 안 봅니다. 리스트가 첫날 죽습니다.

**게이트: `품절일수 ≥ 30` AND `60일 판매 = 0` → 앱 진입 금지, `단종심사` 큐로.**

| | 건수 |
|---|---:|
| 판단 대상 (게이트 통과) | **351** |
| 그중 60일 5개 이상 — 온라인팀 우선순위 | **134** |
| 단종심사 큐 (일괄 처리) | 1,671 |
| 일 신규 유입 추정 | 10~30 |

351건은 사람이 소화 가능하고, 1,671건은 월 1회 카테고리별 일괄 단종으로 텁니다.

---

## 4. 상태 전이

```
시트(매일 재생성)
      │ 읽기
      ▼
  [게이트] ──▶ 단종심사 큐 (30일+ & 60일 판매 0)
      │
      ▼
  판단대기 ◀──────────────────┐
      │                        │ 재검토일 도래
      ├─ 발주예정 ─┐           │
      ├─ 수입검토 ─┤           │
      ├─ 보류 ─────┼───────────┘
      └─ 단종 ─────┼──▶ 종결/단종 ──▶ T1.라이프사이클=단종
                   │                    └─▶ 시트·Slack에서도 제외
                   ▼
              결정됨 ──발주 등록──▶ 입고대기
                                      │
                    이카운트 재고 0→양수 ──▶ 종결/입고확인
                    입고예정일 초과 ──────▶ 🔔 지연 알림
```

`즉시조치=성수점이동`은 이 흐름과 **병렬**입니다. 건을 닫지 않습니다.

---

## 5. 팀별 사용

각 팀이 **무엇을 보고 · 무엇을 입력하고 · 언제 알림을 받는지**입니다.
입력 컬럼이 없는 팀은 화면만 씁니다.

### 물류재고팀 — 재고 트래킹

| | |
|---|---|
| 보는 것 | 전 창고 재고 · 입고대기 목록 · 성수점 이동 요청 · 샘플 입고 예정 |
| 읽는 테이블 | T5 전체 · T3(`상태=입고대기`) · T2(`공정=샘플입고`) |
| 입력 | T3 `이동수량` `이동일` · T2 샘플입고 `완료일` · T1 `입고단가` |
| 알림 | 입고예정일 초과 · 마이너스 재고 발생 · 샘플 입고 D-3 |

온라인팀이 성수점 이동을 요청하면 여기 큐에 뜹니다. 수량을 적고 이동일을 찍으면
온라인 재고가 채워지고, **건은 닫히지 않은 채 발주 판단으로 남습니다.**

### 온라인팀 — 품절 판단 · 오픈

| | |
|---|---|
| 보는 것 | 판단대기 351건 (진입사유·60일/7일 판매·DOI 정렬) · 오늘 오픈 예정 |
| 읽는 테이블 | T3 `상태=판단대기` + T5 조인 · T2(`공정=온라인등록·오픈`) |
| 입력 | T3 `즉시조치` `보충결정` `재검토일` · T2 온라인등록 `완료일` |
| 알림 | 신규 품절 발생 · 재검토일 도래 · 상세페이지 완료(내 차례) · 인기상품 7일+ 품절 |

**하루 화면이 이겁니다.** 판단대기 목록에서 각 건에 조치를 찍으면 리스트가 줄어듭니다.
발주·수입 분류는 T1 `조달유형`이 미리 채워 놓으므로 온라인팀은 **수량과 시급성만 판단**합니다.

### 디자인팀 — 제작제품 발주

| | |
|---|---|
| 보는 것 | `조달유형=자체제작` 중 발주예정 건 · 내 공정 마감일 |
| 읽는 테이블 | T3(`보충결정=발주예정` ∩ T1.조달유형=자체제작) · T2(`공정=기획확정, 발주·생산`) |
| 입력 | T3 `발주일` `발주수량` · T2 기획확정·발주생산 `완료일` `산출물` |
| 알림 | 신규 발주예정 배정 · 발주 착수일 D-7 (오픈일 역산) |

자체제작 리드타임 35일 + 출시 준비 17일 = **오픈 52일 전 발주 착수**가 자동으로 표시됩니다.

### 포토 — 촬영

| | |
|---|---|
| 보는 것 | 샘플 입고 완료된 촬영 대기 품목 · 촬영 마감일 순 |
| 읽는 테이블 | T2(`공정=촬영`, 선행 샘플입고 완료) |
| 입력 | T2 촬영 `담당자` `완료일` `산출물`(원본 링크) |
| 알림 | 샘플 입고 완료 = 촬영 착수 가능 · 촬영 마감 D-3 · 마감 초과 |

**선행 조건이 안 끝난 품목은 목록에 안 뜹니다.** 샘플 없이 촬영 요청이 오는 상황이 없어집니다.

### 컨텐츠팀 — 텍스트

| | |
|---|---|
| 보는 것 | 텍스트 작성 대기 품목 · 기획 정보 · 마감일 |
| 읽는 테이블 | T2(`공정=텍스트`) · T1(품목명·브랜드·카테고리) |
| 입력 | T2 텍스트 `담당자` `완료일` `산출물`(문서 링크) |
| 알림 | 텍스트 착수 가능 · 마감 D-3 · 마감 초과 |

촬영과 **병렬**입니다. 촬영을 기다릴 필요가 없습니다.

### 컨텐츠디자이너 — 상세페이지

| | |
|---|---|
| 보는 것 | **촬영·텍스트 양쪽 완료된** 품목만 · 두 산출물 링크 |
| 읽는 테이블 | T2(`공정=상세페이지`, 선행 촬영·텍스트 모두 완료) |
| 입력 | T2 상세페이지 `담당자` `완료일` `산출물`(시안 링크) |
| 알림 | 촬영·텍스트 둘 다 완료된 순간 · 마감 D-3 · 선행 지연으로 내 일정 밀림 |

**여기가 병목입니다.** 지금은 두 팀 산출물이 언제 오는지 몰라 대기 시간이 그냥 흐릅니다.
선행 완료 시점에 알림이 오고, 선행이 밀리면 내 마감도 같이 밀린 게 보입니다.

### 마케팅팀 — 마케팅 준비

| | |
|---|---|
| 보는 것 | 오픈 예정 캘린더 (D-30 ~ D-0) · 촬영 완료 이미지 · 부스트 대상 |
| 읽는 테이블 | T2(`공정=마케팅준비`) · T2 전체(오픈일순) · T3 재고소량·판매없음 중 60일 5개 이상 |
| 입력 | T2 마케팅준비 `담당자` `완료일` `산출물` |
| 알림 | 오픈 D-30 (기획 착수) · 이미지 확보 완료 · 부스트 대상 신규 발생 |

두 갈래입니다. **신규 출시**는 T2 오픈 캘린더에서, **기존 상품 부스트**는 온라인팀 기준
"60일 판매 5개 이상 → 마케팅 부스트·상세페이지 재정비"에 해당하는 건이 자동으로 올라옵니다
(7월 19일 기준 재고소량·판매없음 25건 중 7건).

---

## 6. 알림 규칙

Slack 발송 조건. 대상 채널은 `기준표`에서 지정합니다.

| 시점 | 조건 | 받는 곳 |
|---|---|---|
| 매일 | 신규 판단대기 발생 | 온라인팀 |
| 매일 | 재검토일 도래 | 결정자 |
| 매일 | 입고예정일 초과 & 미입고 | 물류재고팀 · 발주자 |
| 매일 | 인기상품(60일 5개+) 7일 이상 품절 | 온라인팀 · 물류재고팀 |
| 매일 | 마이너스 재고 발생 | 물류재고팀 |
| 공정 완료 시 | 후행 공정 담당팀에 "착수 가능" | 해당 팀 |
| 매일 | 공정 마감 D-3 · 마감 초과 | 담당자 |
| 주 1회 | 공정 지연으로 오픈일 위험 | 온라인팀 · 해당 팀 |
| 월 1회 | 단종심사 큐 잔량 | MD |

**공정 완료 → 후행 착수 알림이 이 앱의 실질 가치입니다.** 나머지는 시트로도 어느 정도 됩니다.

---

## 7. 정해야 할 것

1. **`발주 검토` 버킷** — 온라인팀 기준 *"발주 검토: 60일 < 7일 판매"* 는 성립하지 않습니다.
   `build_sales_history.py`에서 60일 판매는 롤링 60일 총합이고 7일 판매는 그 안의 부분합이라
   **항상 60일 ≥ 7일**입니다. 나머지 둘(발주 필요 = 지속 판매, 발주 보류 = 최근 7일에만 판매)은
   의도대로 동작합니다. 빈 버킷을 무엇으로 채울지 결정이 필요합니다.
   제안: 수량이 아니라 **속도 비교** — 7일 일평균 대 60일 일평균이 1.2배 이상이면 가속(발주 필요),
   0.8배 미만이면 감속(발주 검토). 지금 데이터로 바로 계산됩니다.
2. **카테고리 목록 확정** — `기준표`의 축입니다. 이게 없으면 카테고리별 기준을 못 겁니다.
3. **조달유형 초기 적재** — 판단 대상 351건에 대해 자체제작/국내사입/해외수입/위탁을 채워야
   발주·수입 자동 분류가 켜집니다. 나머지는 점진 적재해도 됩니다.
4. **이카운트 품목코드 = Cafe24 `custom_variant_code`인지 확정** — `ecount_probe.py` 결과로
   검증이 필요합니다. 다르면 매핑표를 T1에 한 컬럼 더 둡니다.
5. **성수점 이동 수량 기준** — 온라인팀 과제. `기준표.성수점이동_최소수량` 초깃값.
6. **단종심사 1,671건 처리 방식** — 카테고리별 일괄인지, 브랜드별인지, 담당 MD 배정인지.
7. **앱 저장소** — 시트에 앱 전용 탭을 더 둘지, 별도 DB로 갈지. T3·T5는 행이 계속 늘어나므로
   시트로는 한계가 옵니다.
