# POV 재고관리 마스터 — 구글 스프레드시트 설계

이카운트(오프라인) 재고 전용. 온라인(카페24) 재고는 별도 시스템이므로 이 시트는 건드리지 않습니다.

**흐름**: 이카운트 재고·판매 데이터 → 스크립트가 매일 원본 탭에 적재 → 계산 탭에서 상태 판정 → 결과물 탭에 뿌림 → `알림설정`을 읽어 슬랙 발송.

---

## 1. 탭 구성 (11개)

사람이 자주 보는 순서로 배치합니다. 원본 탭이 앞에 있으면 매일 그걸 지나쳐야 합니다.

| # | 탭 | 성격 | 탭색 | 누가 쓰나 |
|---|---|---|---|---|
| 0 | `읽는 법` | 사용 안내 · 범례 | 검정 | 처음 보는 사람 |
| 1 | `대시보드` | 오늘의 작업 큐 | 검정 | 전원 · 매일 |
| 2 | `관리팀_전체재고` | 창고별 전체 재고 | 검정 | 관리팀 |
| 3 | `디자인팀_발주필요` | 제작 발주 대상만 | 검정 | 디자인팀 |
| 4 | `악성재고` | 과잉·장기 미판매 | 회색 | 관리팀 · 주간 |
| 5 | `악성품절` | 재발주/단종 판단 | 회색 | 관리팀 · 월간 |
| 6 | `알림설정` | 슬랙 발송 대상 지정 | 노랑 | 담당자가 직접 수정 |
| 7 | `일별재고이력` | 계산 이력 | 연회색 | 스크립트 |
| 8 | `품목마스터` | 품목↔브랜드↔조달유형 매핑 | 연회색 | 사람이 가끔 수정 |
| 9 | `RAW_재고현황` | 이카운트 원본 | 연회색 | 스크립트 |
| 10 | `RAW_판매현황` | 이카운트 원본 | 연회색 | 스크립트 |

**쓰기 방향이 반대인 탭이 둘 있습니다.** `품목마스터`와 `알림설정`은 사람이 쓰고 스크립트가 읽습니다. 나머지는 스크립트가 쓰고 사람이 읽습니다. 스크립트가 이 둘을 덮어쓰면 안 됩니다.

---

## 1-1. `읽는 법` 탭 — 전사 배포의 전제

전사에서 보는 시트라면 **처음 여는 사람이 아무도 설명해 주지 않아도 쓸 수 있어야** 합니다. 동그라미 6색이 무슨 뜻인지, `DOI`·`조달유형`·`초과주문`이 무슨 말인지 시트 안 어디에도 없으면 만든 사람만 아는 시트가 됩니다.

맨 앞 탭에 다섯 절로 담습니다.

| 절 | 내용 |
|---|---|
| ① 하루에 이렇게 씁니다 | 대시보드 열기 → 위 블록부터 처리 → 자세히 볼 땐 출처 탭으로. 3줄 |
| ② 어느 탭을 보면 되나 | 탭 · 누가 · 언제 · **수정 가능 여부** · 무엇을 보나 |
| ③ 동그라미가 뜻하는 것 | 신호 · 뜻 · 언제까지 · 상태 이름 · 무엇을 하나 |
| ④ 표에 나오는 말 | DOI · 리드타임 · 조달유형 · 총재고 · 7일/90일 판매 · 재고금액 · 우선순위 |
| ⑤ 지켜 주세요 | 수정 금지 탭, 사람이 쓰는 탭, 숫자가 이상할 때 |

두 가지가 특히 중요합니다.

**②의 `수정` 컬럼** — 읽기만 / 메모만 / 직접 수정 / 수정 금지. 전사 배포에서 가장 먼저 생기는 사고가 "누가 값을 덮어썼다"인데, 어느 탭이 자동으로 다시 쓰이는지 모르면 막을 수 없습니다.

**③의 `무엇을 하나` 컬럼** — 색의 뜻만 적으면 반쪽입니다. 🔴을 보고 무엇을 해야 하는지까지 적혀야 시트가 혼자 굴러갑니다. 예: 초과주문은 "재고가 음수 — 전산 오류일 가능성. 이카운트 전표를 먼저 확인".

---

## 2. 공통 레이어 — 10개 탭 전부 동일

머리글이 3층입니다. 탭마다 달라지는 것은 조건부서식뿐입니다.

| 행 | 역할 | 스타일 |
|---|---|---|
| 1행 | **배너 — 지시문** | 배경 `#1A1A1A` · 흰 글씨 Bold 10pt · 가운데 · 전체 병합 · 30px |
| 2행 | **요약 — 판정 조건 · 건수 / 갱신 시각** | 배경은 탭 성격에 따라 · 좌측 판정조건+건수, 우측 갱신시각 · 22px |
| 3행 | 헤더 | 배경 `#F1F1F1` · 글씨 `#222222` Bold · 하단 `#333333` 굵은 테두리 · 34px |
| 4행~ | 데이터 | 줄무늬 `#FFFFFF` / `#FAF8F6` · 24px |

**1행은 설명이 아니라 지시문입니다.** "관리팀용 마스터 시트 — 창고별 재고를 보고 판단"이 아니라 **"RAW 갱신 후 [재고 갱신] 버튼을 눌러 주세요"**. 매일 여는 사람에게 필요한 건 이 탭이 뭔지가 아니라 지금 뭘 해야 하는지입니다. 탭 성격 설명은 2행이 대신합니다.

**2행에는 판정 조건을 괄호로 적습니다.** `발주 필요 — 18개 (초과주문 · 품절 · DOI < 임계값 · 재고소량)`. 왜 이 18개가 여기 들어왔는지를 목록 위에서 바로 알 수 있어야 합니다. 우측에는 실제 갱신 시각을 찍습니다 — "매일 자동"이라고만 적으면 오늘 돌았는지 어제 것인지 알 수 없습니다.

2행 배경은 탭 성격을 따릅니다.

| 탭 | 2행 배경 / 글씨 |
|---|---|
| 디자인팀_발주필요 | 분홍 `#F9D0D0` / `#CC0000` |
| 악성재고 | 파랑 `#D6EAF8` / `#1565C0` |
| 알림설정 | 살구 `#FDDCCC` / `#B35C00` |
| 관리팀_전체재고 · 대시보드 | 연회색 `#EFEFEF` / `#333333` |
| 악성품절 · 원본·계산 탭 | 연회색 `#EFEFEF` / `#999999` |

| 기타 | 값 |
|---|---|
| 본문 텍스트 | `#333333` (순수 검정 금지) |
| 셀 테두리 | 하단 `#DDDDDD` 얇은 선만 |
| 격자선 | 끄기 |
| 틀 고정 | 3행 + 품목명 컬럼까지 |

배너를 검정으로 올리고 헤더를 밝게 내린 이유: 틀고정으로 세 줄이 항상 붙어 있는데 배너와 헤더가 둘 다 어두우면 화면 상단에 두꺼운 띠가 생깁니다. 배너가 탭 제목, 2행이 현황, 헤더는 물러나는 순서가 가볍습니다.

### 머리글 문구 규칙

**1행 배너 — 이 탭에서 무엇을 하는가.** 탭 이름은 반복하지 않습니다(탭 줄에 이미 있습니다). 서술형 한 문장으로 끝내고, 원본·계산 탭은 금지문으로 닫습니다.

| 탭 | 1행 |
|---|---|
| 대시보드 | 오늘 처리할 것을 위에서부터 순서대로 · RAW 갱신 후 [재고 갱신] 버튼을 눌러 주세요 |
| 관리팀_전체재고 | 창고별 재고를 보고 발주와 매장 간 이동을 결정합니다 |
| 디자인팀_발주필요 | 제작 발주가 필요한 품목만 모았습니다 · 위에서부터 착수 |
| 악성재고 | 재고가 쌓여 돈이 묶인 품목입니다 · 주간 검토 |
| 악성품절 | 되살릴지 접을지 결정할 품목입니다 · 월간 검토 |
| 알림설정 | 슬랙으로 받을 목록과 브랜드를 여기서 켜고 끕니다 |
| 일별재고이력 | 모든 상태 판정이 여기서 나옵니다 · 직접 수정 금지 |
| 품목마스터 | 신상품이 생기면 여기에 추가합니다 |
| RAW_재고현황 | 이카운트 재고 원본입니다 · 직접 수정 금지 |
| RAW_판매현황 | 이카운트 판매 원본입니다 · 직접 수정 금지 |

갱신 버튼 안내는 대시보드 한 곳에만 둡니다. 탭마다 반복하면 문구가 아니라 배경이 됩니다.

**2행 요약 — 지금 몇 개이고 무슨 기준인가.** 좌측은 건수와 판정 조건, 우측은 실제 갱신 시각. "매일 자동"이라고만 적으면 오늘 돈 건지 어제 것인지 알 수 없습니다.

| 탭 | 2행 좌측 |
|---|---|
| 대시보드 | 결과물 탭 4개에서 자동 집계 · 블록별 판정 조건과 정렬 근거는 섹션 우측 표기 |
| 관리팀_전체재고 | 전체 31개 — 🔴 10 · 🟠 4 · 🟡 3 · 🔵 3 · ⚫️ 1 · 🟢 10 |
| 디자인팀_발주필요 | 18개 — 초과주문 · 품절 · DOI 임계값 미만 · 재고소량 |
| 악성재고 | 10개 — 미판매 90일 이상 · 재고금액 큰 순 |
| 악성품절 | 6개 — 재고 0 · 미판매 90일 이상 |
| 알림설정 · 품목마스터 | 사람이 쓰고 스크립트가 읽습니다 · 갱신해도 덮어쓰지 않습니다 |
| 일별재고이력 | 품목당 하루 한 행씩 누적 |
| RAW_재고현황 | 총재고와 창고별 수치가 전부 여기서 나옵니다 |
| RAW_판매현황 | 전일 판매 전표 · DOI 계산의 분모 |

`관리팀_전체재고`는 건수 대신 **상태 분포를 동그라미로** 적습니다. 오늘 구성이 한 줄로 보이고, 아래 목록의 동그라미와 같은 언어라 따로 읽을 게 없습니다.

### 컬럼 헤더 명명 규칙

1. **단위는 괄호로 뒤에** — `리드타임(일)` `DOI(소진일)` `품절(일)` `미판매(일)`
2. **기간은 앞에** — `7일 판매` `90일 판매` (`최근7일판매량` → 군더더기 제거)
3. **군더더기 제거** — `재고수량` → `재고`, `조치방안` → `조치`, `입고수량(계산)` → `입고`, `재고평가금액` → `재고금액`
4. **RAW 탭은 이카운트 원본 필드명 그대로** — 이름을 바꾸면 적재 매핑이 깨집니다

`DOI`는 약어라 처음 보면 못 읽습니다. 전사에서 보는 시트이므로 **`DOI(소진일)`** 로 약어와 뜻을 함께 노출합니다. 재고 알림 봇이 `DOI`를 쓰므로 약어를 버리지는 않고, 괄호로 풀어 두 쪽 모두 읽히게 합니다.

| 탭 | 헤더 |
|---|---|
| 관리팀_전체재고 | 브랜드 · 품목코드 · 품목명 · 조달유형 · 상태 · 리드타임(일) · 창고 4 · 총재고 · 전일재고 · 입고 · 출고 · 7일 판매 · DOI(소진일) · 조치 · 메모 |
| 디자인팀_발주필요 | 브랜드 · 품목코드 · 품목명 · 조달유형 · 상태 · 우선순위 · 리드타임(일) · 재고 · DOI(소진일) · 7일 판매 · 90일 판매 · 품절(일) · 조치 · 메모 |
| 악성재고 | 브랜드 · 품목코드 · 품목명 · 재고 · 최근판매일 · 미판매(일) · 입고단가 · 재고금액 · 조치 · 메모 |
| 악성품절 | 브랜드 · 품목코드 · 품목명 · 재고 · 최근판매일 · 미판매(일) · 품절(일) · 조달유형 · 리드타임(일) · 조치 · 메모 |
| 일별재고이력 | 날짜 · 브랜드 · 품목코드 · 품목명 · 상태 · 우선순위 · 전일재고 · 재고 · 출고 · 입고 · 7일 판매 · 90일 판매 · DOI(소진일) · 품절(일) · 조치 |
| 대시보드 | 순위 · 브랜드 · 품목명 · 조달유형 · 상태 · 재고 · 7일 판매 · DOI(소진일) · 경과(일) · 재고금액 · 조치 |

### 폰트

구글 시트 `글꼴 > 더 많은 글꼴`에서 **Noto Sans KR**, **Roboto Mono** 추가.

| 영역 | 글꼴 | 정렬 |
|---|---|---|
| 배너 · 헤더 | Noto Sans KR 10pt Bold | 가운데 |
| 품목명 · 브랜드 · 조치방안 · 메모 | Noto Sans KR 10pt | 좌측 |
| 품목코드 · 조달유형 | Noto Sans KR 10pt | 가운데 |
| **상태** | Noto Sans KR 10pt | **좌측** |
| 모든 숫자 | **Roboto Mono 10pt** | 우측 |

숫자를 고정폭으로 두면 자릿수가 세로로 정렬돼 재고량 스캔 속도가 크게 올라갑니다. 체감이 가장 큰 항목입니다.

### 숫자 서식

| 대상 | 코드 |
|---|---|
| 수량 · 판매량 | `#,##0;-#,##0;"–"` |
| DOI · 경과일 | `0"일";;"–"` |
| 금액 | `#,##0"원";;"–"` |
| 날짜 | `yyyy-mm-dd` |

0을 `–`로 바꾸는 게 포인트입니다. 0이 빽빽하면 "재고 없음"이 눈에 안 들어옵니다.

### 열 너비 / 줄바꿈

품목명 220px · 메모·조치방안 200px · 브랜드·조달유형·상태 90px · 품목코드 100px · 숫자 75px · 창고 각 95px.
줄바꿈은 **잘림(CLIP)**, `메모`·`조치방안`만 `WRAP`. 행 높이가 들쭉날쭉하면 스캔이 어렵습니다.

---

## 3. 색과 상태

**색은 칠하는 게 아니라 예외를 알리는 신호입니다.** 정상 품목에 색을 칠하면 목록 대부분이 덮여서 정작 위험 품목이 안 보입니다. 정상은 무색으로 둡니다.

포인트 컬러는 3개. 배경은 파스텔로 두고 **글자색만 선명하게** 올립니다. 색의 강도는 채도가 아니라 면적이 결정합니다 — 같은 빨강도 셀을 채우면 시끄럽고 숫자 몇 글자에 얹으면 정확히 꽂힙니다.

| 신호 | 뜻 | 상태 | 글자색 |
|---|---|---|---|
| 🔴 | 지금 처리 | 초과주문 · 위험 · 품절-신규 | `#CC0000` Bold |
| 🟠 | 이번 주 처리 | 주의 · 품절-지속 | `#B35C00` Bold |
| 🟡 | 확인 필요 | 재고소량 | `#B36B00` Bold |
| 🟢 | 정상 | 정상 | `#333333` |
| 🔵 | 과잉 | 과잉 | `#1565C0` Bold |
| ⚫️ | 끝 | 품절-장기 | `#999999` |

**셀 배경은 칠하지 않습니다.** 동그라미가 색을 맡은 이상 배경까지 칠하면 같은 신호를 두 번 보내는 것이고, 목록 절반이 파스텔로 덮여 정작 동그라미가 묻힙니다. 색은 **동그라미와 글자색**에만 씁니다.

배경을 쓰는 곳은 셋뿐입니다 — 1행 배너(검정), 2행 요약, 데이터 줄무늬(`#FAF8F6`). 전부 구조를 나타내는 것이지 상태를 나타내는 게 아닙니다.

**시간 축 하나로 정렬됩니다.** 지금 / 이번 주 / 언젠가 / 없음. 색을 보면 언제 손대야 하는지가 바로 나옵니다.
줄무늬 `#FAF8F6`는 🔴 배경과 명도차가 충분해 "이 행이 위험인가 짝수 행인가"를 다시 생각할 일이 없습니다.

### 상태 어휘 (9단계)

`상태` 컬럼에는 **동그라미 + 상태명**을 함께 넣습니다. 색·굵기 외에 형태로도 구분되므로 색약 상태에서도 읽히고, 복사·내보내기·모바일에서도 신호가 남습니다.

| 신호 | 상태 | 조건 |
|---|---|---|
| 🔴 | 초과주문 | 재고 < 0 |
| 🔴 | 위험 | DOI < 위험선 |
| 🔴 | 품절-신규 | 재고 0 · 0–9일 |
| 🟠 | 주의 | DOI < 주의선 |
| 🟠 | 품절-지속 | 재고 0 · 10–29일 |
| 🟡 | 재고소량 | 재고 1–5 · 7일 판매 0 |
| 🟢 | 정상 | 그 외 |
| 🔵 | 과잉 | DOI > 180일 |
| ⚫️ | 품절-장기 | 재고 0 · 30일+ |

**동그라미만 씁니다.** 모양이 전부 같아야 눈이 모양을 해독하지 않고 색만 봅니다. 🚨🐢👀 같은 서로 다른 글리프를 섞으면 글자 폭도 시각적 무게도 제각각이라, 색을 읽기 전에 모양을 먼저 해석하게 됩니다.

**동그라미 색 = 배경색.** 🔴엔 분홍, 🟠엔 살구, 🟡엔 노랑, 🟢은 무색. 점과 배경이 같은 계열이라 색 체계가 하나로 유지됩니다.

**품절 3단계에 순서가 생깁니다.** 갓 품절된 건 지금 채우면 매출을 살릴 수 있어 🔴, 10–29일이면 🟠, 30일 넘으면 이미 죽은 상품이라 단종 판단 대상이므로 🟡. 이전의 🆕 / 🔴 / 🐢 는 셋 사이에 위계가 없어 모양만으로는 어느 쪽이 급한지 알 수 없었습니다.

**⚫️만 변이 선택자가 필요합니다 — `U+26AB` + `U+FE0F`.** 단독으로 쓰면 폰트에 따라 속이 빈 동그라미(●)나 회색 원으로 떨어집니다. 나머지 다섯(🔴 U+1F534 · 🟠 U+1F7E0 · 🟡 U+1F7E1 · 🟢 U+1F7E2 · 🔵 U+1F535)은 이모지 전용 문자라 선택자가 필요 없습니다.

편집기에서는 선택자가 눈에 보이지 않아 복사하다 떨어져 나가기 쉬우므로, 스크립트에는 `"⚫"` 대신 **`"\u26AB\uFE0F"`** 로 적습니다.

**`상태` 컬럼은 좌측 정렬입니다.** 가운데 정렬하면 상태명 길이(`위험` 2자 ~ `품절-신규` 5자)에 따라 동그라미가 좌우로 흔들려 세로줄이 어긋납니다. 좌측 정렬 + 들여쓰기 1이면 모든 동그라미가 같은 x 위치에 서고, 그래야 색만 훑는 게 가능해집니다.

**🔵는 부족의 반대 축입니다.** 🔴🟠🟡이 "재고가 모자라다"의 단계라면 🔵는 "너무 많다"입니다. 시간 축이 아니라 반대 방향이라 파랑이 맞습니다. 돈은 묶여 있지만 오늘 아무 일도 일어나지 않으므로, 금액 기준 우선순위는 `악성재고` 탭이 따로 잡습니다.

`품절-장기`(⚫️)는 30일 넘게 품절이라 이미 죽은 상품입니다. 품절 3단계가 🔴 → 🟠 → ⚫️ 로 색이 꺼져가며 완결됩니다 — 급함 → 이번 주 → 끝.

**⚫️는 반드시 행 전체를 회색으로 눌러야 합니다.** 이 세트에서 대비가 가장 센 글리프라, 파스텔 배경 위에 그냥 두면 빨강보다 더 튑니다. 글자를 회색으로 깔고 배경을 비워야 검정 점이 경보가 아니라 마침표로 읽힙니다. 품절-장기 품목이 수백 개로 늘어나면 검정 점이 열을 이루므로, 그때는 `악성품절` 탭으로 몰고 `관리팀_전체재고`에서는 감추는 편이 낫습니다.

**🟡은 확인이 필요한 것입니다.** `재고소량`은 재고가 몇 개 안 남았는데 7일간 판매가 0이라 DOI 자체가 계산되지 않는 구간입니다 — 노출이 막힌 건지 정말 안 팔리는 건지 사람이 봐야 합니다. `품절-장기`는 30일 넘게 품절이라 재발주할지 단종할지 판단이 필요합니다. 둘 다 자동 판정이 안 되는 것들이라 눈에 남겨 둡니다.

**과잉이 파랑에서 노랑으로 옮겨졌습니다.** 신호등에 파랑이 없어 부족 ↔ 과잉을 반대 축으로 두던 구도는 사라집니다. 대신 모든 상태가 "언제 처리하나"라는 단일 시간 축에 정렬됩니다.

---

## 4. 조건부서식---

## 4. 조건부서식 — 상태 문자열 기반

규칙을 DOI 임계값이 아니라 `상태` 컬럼 값으로 겁니다. 상태 판정은 스크립트가 이미 하므로 서식이 임계값을 두 번 계산할 이유가 없습니다. **임계값을 바꿔도 조건부서식은 손댈 필요가 없습니다.**

공통 수식 (E열 = 상태, 데이터 4행부터):

```
F_LOW  =OR(ISNUMBER(SEARCH("초과주문",$E4)),ISNUMBER(SEARCH("위험",$E4)),ISNUMBER(SEARCH("품절-신규",$E4)))
F_WARN =OR(ISNUMBER(SEARCH("주의",$E4)),ISNUMBER(SEARCH("품절-지속",$E4)))
F_OVER =ISNUMBER(SEARCH("재고소량",$E4))
F_BLUE =ISNUMBER(SEARCH("과잉",$E4))
F_DIM  =ISNUMBER(SEARCH("품절-장기",$E4))
```

정상은 아무 규칙도 걸지 않습니다.

### `디자인팀_발주필요`
A브랜드 B품목코드 C품목명 D조달유형 **E상태** F우선순위 G리드타임 H재고수량 I DOI J최근7일 K최근90일 L품절경과일 M조치방안 N메모

| # | 범위 | 수식 | 서식 |
|---|---|---|---|
| 1 | `E4:E`, `I4:I`, `M4:M` | `F_LOW` | 글자 `#CC0000` Bold |
| 2 | `E4:E`, `I4:I`, `M4:M` | `F_WARN` | 글자 `#B35C00` Bold |
| 3 | `E4:E`, `I4:I`, `M4:M` | `F_OVER` | 글자 `#B36B00` Bold |
| 4 | `A4:N` | `F_DIM` | 글씨 `#999999` |
| 5 | `C4:C` | `F_LOW` | **Bold** (색 없음) |

규칙 4가 핵심입니다. 행 전체를 칠하지 않고도 품목명만 굵어지므로, 왼쪽 열을 훑는 것만으로 위험 행이 잡히면서 표는 조용하게 유지됩니다. **색은 `상태 / DOI / 조치방안` 3개 셀에만.**

### `관리팀_전체재고`
A브랜드 B품목코드 C품목명 D조달유형 **E상태** F리드타임 G~J창고4 K총재고 L전일재고 M입고 N출고 O최근7일 P DOI Q조치방안 R메모

| # | 범위 | 수식 | 서식 |
|---|---|---|---|
| 1 | `E4:E`, `P4:P`, `Q4:Q` | `F_LOW` | 분홍 / `#CC0000` Bold |
| 2 | `E4:E`, `P4:P`, `Q4:Q` | `F_WARN` | 노랑 / `#B36B00` Bold |
| 3 | `E4:E`, `P4:P`, `Q4:Q` | `F_OVER` | 노랑 / `#B36B00` Bold |
| 4 | `E4:E`, `P4:P`, `Q4:Q` | `F_BLUE` | 파랑 / `#1565C0` Bold |
| 5 | `A4:R` | `F_DIM` | 글씨 `#999999` |
| 6 | `C4:C` | `F_LOW` | **Bold** |
| 7 | `G4:J` | `=AND(G4=0,$K4>0)` | 글씨 `#CCCCCC` |

규칙 7이 **창고이동 판단용**입니다. 총재고는 있는데 특정 매장만 0인 칸이 흐려지므로, 가로로 훑으면 어디로 보내야 하는지가 바로 보입니다.

> **`악성재고`만 예외로 배경을 씁니다.** 이 탭에는 `상태` 컬럼이 없어 동그라미가 없으므로, 배경이 유일한 신호입니다. 상태 컬럼을 추가하면 여기도 배경을 걷을 수 있습니다.

### `악성재고`
A브랜드 B품목코드 C품목명 D재고수량 E최근판매일 F미판매경과일 G입고단가 H재고평가금액 I조치방안 J메모

| # | 범위 | 수식 | 서식 |
|---|---|---|---|
| 1 | `F4:F`, `H4:H`, `I4:I` | `=$F4>=180` | 노랑 / `#B36B00` Bold |
| 2 | `F4:F`, `H4:H` | `=AND($F4>=90,$F4<180)` | 노랑 / `#B36B00` Bold |
| 3 | `H4:H` | `=$H4>=1000000` | **Bold** |

진짜 신호는 "오래됨"이 아니라 **오래됨 × 금액**입니다. 규칙 3이 돈이 크게 묶인 품목을 굵게 만들어 폐기·프로모션 우선순위를 잡아줍니다.

### 나머지 탭
조건부서식 없음. 공통 레이어만 적용. `악성품절`은 전체 글씨를 `#999999`로 — 가라앉아 있어야 하는 탭입니다.

### DOI 계산

```
=IF(OR($O4=0, $K4<=0), "", ROUND($K4/($O4/7), 0))
```

재고가 0 **이하**일 때 빈칸입니다. 초과주문(음수 재고)에서 DOI가 음수로 나오면 정렬이 망가집니다.

---

## 5. `대시보드` — 작업 큐

요약 숫자와 추이 그래프는 넣지 않습니다. "긴급발주 9건"을 봐도 할 수 있는 행동이 없고 결국 탭으로 넘어가 목록을 봐야 하므로 단계만 늘어납니다. **처리할 목록을 모아둔 큐**입니다.

### 공통 컬럼 그리드 (블록 전부 동일)

`순위` `브랜드` `품목명` `조달유형` `상태` `재고수량` `최근7일판매량` `DOI` `경과일` `재고평가금액` `조치방안`

블록마다 컬럼을 바꾸지 않습니다. 하나의 열 그리드를 공유해야 위아래로 스크롤하며 같은 위치에서 같은 정보를 읽습니다.

### 블록 구조

```
1행    [배너 검정]  대시보드 — 오늘 처리할 목록만 모아둔 탭
2행    [요약 회색]  결과물 탭 4개에서 매일 자동 집계     |  마지막 갱신: 2026-07-28 09:13
4행    [섹션]  오늘 조치 필요 — 15건   |   출처: 디자인팀_발주필요 · 정렬: 우선순위 → 재고 오름차순 · 상위 20
5행    헤더
6행~   데이터
(빈 행) 다음 블록…
```

섹션 행은 A:E 병합에 제목·건수·행동 지시(좌측), F:K 병합에 출처·정렬 근거(우측).

```
오늘 조치 필요 — 상위 20 / 전체 47개   →  지금 발주해야 리드타임 커버
                                    출처: 디자인팀_발주필요 · 정렬: 우선순위 → 재고 오름차순
```

세 가지가 한 줄에 들어갑니다.

- **상한과 전체 건수를 같이** — "15건"만 적으면 이게 전부인지 잘린 건지 모릅니다. "상위 20 / 전체 47개"라야 32개를 못 보고 있다는 걸 압니다. 전체가 상한보다 크면 스크립트가 "…외 27개는 해당 탭 참고"를 마지막 행에 붙입니다.
- **행동 지시 한 줄** — 같은 블록의 품목은 할 일이 같습니다. 행마다 반복할 이유가 없습니다.
- **정렬 근거** — 다른 사람이 열어도 왜 이 순서인지 묻지 않아도 됩니다.

### 6개 블록

| # | 블록 | 출처 | 정렬 | 상한 | 행동 지시 |
|---|---|---|---|---|---|
| 1 | 오늘 조치 필요 | 디자인팀_발주필요 | 우선순위 → 재고 오름차순 | 20 | 지금 발주해야 리드타임 커버 |
| 2 | 3일 이상 품절 | 디자인팀_발주필요 | 품절경과일 내림차순 | 10 | 즉시 재입고 검토 |
| 3 | 창고이동 검토 | 관리팀_전체재고 | 결품 매장 수 내림차순 | 20 | 결품 매장으로 재고 이동 |
| 4 | 판매 속도 상위 | 관리팀_전체재고 | 최근 7일 판매량 내림차순 | 10 | 재입고 우선순위 확인 |
| 5 | 악성재고 정리 | 악성재고 | 재고평가금액 내림차순 | 10 | 프로모션 · 번들 · 폐기 판단 |
| 6 | 악성품절 단종 판단 | 악성품절 | 미판매경과일 내림차순 | 10 | 재발주 여부 · 단종 확정 |

순서는 **긴급 → 판단**입니다. 위 3개는 오늘 처리할 것, 4번은 재입고 순서, 5·6번은 주간·월간 판단입니다.

```
1 =QUERY(디자인팀_발주필요!A4:N,"select A,C,D,E,H,J,I,L,M where E is not null order by F asc, H asc limit 20",0)
2 =QUERY(디자인팀_발주필요!A4:N,"select A,C,D,E,H,J,I,L,M where L >= 3 order by L desc limit 10",0)
4 =QUERY(관리팀_전체재고!A4:R,"select A,C,D,E,K,O,P,Q order by O desc limit 10",0)
5 =QUERY(악성재고!A4:J,"select A,C,D,F,H,I order by H desc limit 10",0)
6 =QUERY(악성품절!A4:K,"select A,C,F,J order by F desc limit 10",0)
```

블록 3(창고이동)은 "창고 4개 중 0인 개수"가 정렬 키라 QUERY만으로는 안 됩니다. `관리팀_전체재고`에 헬퍼 컬럼을 두거나 스크립트에서 계산합니다.

### `조치방안` 컬럼은 다를 때만 채웁니다

블록마다 행동 지시가 이미 있으므로, 같은 블록 안에서 할 일이 동일하면 행마다 반복하지 않습니다. **품목별로 실제로 다를 때만** 채웁니다 — 창고이동은 어느 매장이 결품인지가 품목마다 다르므로 채우고, 발주 대상은 블록 지시로 충분하므로 비웁니다. 정상 품목도 비어 있습니다. 할 일이 없거나 이미 위에 적혀 있으면 비어 있는 게 맞습니다.

### 슬랙에는 있지만 시트에는 넣지 않는 것

재고 알림 봇은 **재고량 구간**을 별도의 색 점(0개 · 1–2개 · 3–5개 · 6개+)으로 표시합니다. **시트에는 넣지 않습니다.** 시트의 동그라미는 이미 상태(심각도)를 나타내고 있어, 같은 자리에 재고량이라는 다른 축을 겹치면 두 개의 색 언어가 충돌합니다. 슬랙은 표가 아니라 문장 나열이라 숫자를 세로로 훑을 수 없어서 점이 필요하지만, 시트의 `재고수량`은 우측 정렬 고정폭 숫자라 그 자체로 이미 읽힙니다. 같은 정보를 두 번 인코딩하면 노이즈만 늘어납니다.

**판매 속도 상위**는 단순 인기 순위가 아닙니다. 같은 행에 `상태`와 `DOI`가 붙어 있어 "잘 팔리는데 재고가 얇은 것"이 위쪽에 몰립니다. `조치방안`은 품목 고유값을 그대로 가져오므로 정상 품목은 빈칸입니다 — 할 일이 없으면 비어 있는 게 맞습니다.

---

## 6. `알림설정` — 슬랙 발송 대상

발송 대상을 **스크립트에 하드코딩하지 않고 시트에서 지정**합니다. 하드코딩하면 "이 브랜드도 알림 받게 해주세요" 한 마디에 매번 개발자가 붙어야 합니다.

**이 탭은 스크립트가 덮어쓰지 않습니다.**

**① 목록 알림** — `항목 · 출처 · 발송(Y/N) · 상한 · 발송시각 · 슬랙 채널 · 메모`
대시보드 6블록이 그대로 행이 됩니다. 매일 보낼 것(오늘 조치·품절·창고이동)과 주간·월간으로 충분한 것(악성재고·악성품절)을 발송시각으로 구분합니다.

**② 브랜드 지정 알림** — `브랜드 · 브랜드코드 · 발송(Y/N) · 대상 상태 · 담당자 · 슬랙 채널 · 메모`
브랜드 담당자가 자기 브랜드만, 지정한 상태일 때만 받습니다. 전체 알림을 모두에게 보내면 곧 아무도 안 봅니다.

`발송`은 Y/N 드롭다운(데이터 확인). `Y`는 굵게, `N`은 `#999999`로 눌러 켜진 것만 도드라지게 합니다. 설정값은 예외 신호가 아니므로 포인트 컬러를 쓰지 않습니다.

---

## 7. 정해야 할 것

1. **DOI 임계값** — 위험선을 무엇으로 잡을지. ① 조달유형별 상수(자체제작 35 / 국내사입 7 / 해외수입 21), ② 품목별 `리드타임(일)` 컬럼(`=AND($I4<>"",$I4<$G4)`), ③ 평탄한 기준(위험 5일 / 주의 5–10일). ②가 정확하고 수식도 단순합니다. 주의선은 위험선 + 버퍼로 두되 배수가 아니라 실제 발주 준비 기간으로 잡는 편이 낫습니다(자체제작 +20일 / 국내사입 +7일 / 해외수입 +14일).
2. **창고 목록 확정** — 현재 4곳(POINT OF VIEW(법인) · THE HYUNDAI SEOUL · 시시호시-수원점 · 신세계 강남-피숀). 이카운트 창고 코드 목록을 뽑아 어떤 창고를 `총재고` 합산 대상으로 볼지 명시해야 합니다.
3. **품절 단계 경계** — 신규 0–9일 / 지속 10–29일 / 장기 30일+. 오프라인 회전 주기에 맞는지 확인.
4. **일별재고이력 블록 추가 여부** — "어제 하루 출고가 7일 평균 대비 급증한 품목". 어느 목록에도 아직 안 잡히지만 갑자기 팔리기 시작한 것들로, 조기 경보로는 가장 빠릅니다.
5. **품목 상세 딥링크** — 이카운트 품목 화면으로 바로 가는 URL이 가능하면 `메모` 앞에 컬럼 추가.
